name: 📊 Update Stats (Safe)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-stats:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: ⚙️ Configurar Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: 📊 Gerar STATS.md
        run: |
          cat <<EOF > STATS.md
          # 📊 Estatísticas do GitHub - Marco Túlio

          ## 📈 Estatísticas Gerais
          ![GitHub Stats](https://github-readme-stats.vercel.app/api?username=MarcoTuliops22&show_icons=true&theme=tokyonight)

          ## 🚀 Linguagens Mais Usadas
          ![Top Languages](https://github-readme-stats.vercel.app/api/top-langs/?username=MarcoTuliops22&layout=compact&theme=tokyonight)

          ---
          *Atualizado automaticamente em $(date '+%d/%m/%Y %H:%M:%S')*
          EOF

      - name: 📤 Commit e Push
        run: |
          git add STATS.md

          if git diff --cached --quiet; then
            echo "✅ Nenhuma alteração detectada"
          else
            git commit -m "📊 Atualizar estatísticas automaticamente"
            git push
          fi
