name: Generate Languages Report

on:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    - cron: '0 8 * * *'  # todo dia às 8h UTC

permissions:
  contents: write

jobs:
  generate-languages:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install PyGithub

      - name: Generate LANGUAGES_REPORT.md
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_USERNAME: ${{ github.repository_owner }}
        run: |
          python - <<PYTHON_CODE
          import os
          import sys
          from github import Github
          from collections import defaultdict
          from datetime import datetime

          token = os.environ.get("GH_TOKEN")
          username = os.environ.get("GITHUB_USERNAME")

          if not token or not username:
              print("Erro: GH_TOKEN ou GITHUB_USERNAME não configurado")
              sys.exit(1)

          g = Github(token)
          user = g.get_user(username)

          repos_data = []
          language_stats = defaultdict(int)
          language_bytes = defaultdict(int)

          print(f"Analisando repositórios de {username}...")

          for repo in user.get_repos():
              try:
                  langs = repo.get_languages()
                  repos_data.append({
                      "name": repo.name,
                      "url": repo.html_url,
                      "languages": langs,
                      "primary_language": repo.language or "Não detectada",
                      "stars": repo.stargazers_count,
                      "updated_at": repo.updated_at.isoformat() if repo.updated_at else ""
                  })
                  for lang, b in langs.items():
                      language_stats[lang] += 1
                      language_bytes[lang] += b
              except Exception as e:
                  print(f"Erro ao processar {repo.name}: {e}")

          # Gerar relatório Markdown
          md = f"# 📊 Relatório de Linguagens - {username}\n\n"
          md += f"**Data:** {datetime.utcnow().strftime('%Y-%m-%d %H:%M:%S')} UTC\n"
          md += f"**Total de repositórios analisados:** {len(repos_data)}\n"
          md += f"**Linguagens únicas encontradas:** {len(language_stats)}\n\n---\n\n"

          md += "## 🏆 Top Linguagens por repositórios\n\n"
          sorted_langs = sorted(language_stats.items(), key=lambda x: x[1], reverse=True)
          for lang, count in sorted_langs:
              md += f"- **{lang}**:
